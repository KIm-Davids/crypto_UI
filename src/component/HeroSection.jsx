import React from 'react'

const HeroSection = () => {
    return (
        <section className="section hero" aria-label="hero">
            <div className="container">
                <div className="hero-content">
                    <h1 className="h1 hero-title">Get Ready to HODL</h1>
                    <p className="hero-text"> The Meme Token That's Taking Over the Internet!</p>
                    <a href="#" className="btn btn-primary">Get started now</a>
                </div>
            </div>
        </section>
    )
}

export default HeroSection